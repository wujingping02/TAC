<view class="box">
    <text class="lable">{{lable}}</text>
    <input disabled="{{disabled}}" class="val {{className}}" placeholder="{{'请输出' + lable}}" value="{{value}}" bindblur="onBlur" id="val" type="{{type}}" label="{{lable}}"/>
</view>
