<view class="headerBox" style="position: {{position || 'fixed'}};background : {{color || '#FFCE00'}}">
  <view wx:if="{{!hideBack}}" bind:tap="back" class="backBox">
    <image class="back" src="../../images/back.png"/>
  </view>
  <text class="title">{{title}}</text>
</view>